sudo: false
language: perl
perl:
   - '5.30'
   - '5.28'
   - '5.26'
   - '5.24'
   - '5.22'
   - '5.20'
   - '5.18'
   - '5.16'
   - '5.14'
env:
   - BUILD=0
   - BUILD=1
matrix:
   fast_finish: true
before_install:
   - 'if [[ $BUILD == 1 && ! -d .build/testing ]]; then unset BUILD; fi'
   - export AUTOMATED_TESTING=1 NONINTERACTIVE_TESTING=1 HARNESS_OPTIONS=j10:c HARNESS_TIMER=1
   - git clone git://github.com/haarg/perl-travis-helper
   - source perl-travis-helper/init
   - build-perl
   - perl -V
   - 'if [[ $BUILD == 0 ]]; then git config --global user.name "TravisCI"; fi'
   - 'if [[ $BUILD == 0 ]]; then git config --global user.email $HOSTNAME":not-for-mail@travis-ci.org"; fi'
   - 'if [[ $BUILD == 0 ]]; then cpanm --verbose --installdeps --notest Pod::Weaver::Section::Contributors; fi'
   - 'if [[ $BUILD == 0 ]]; then cpanm --verbose --installdeps --notest Pod::Elemental::Transformer::List; fi'
   - 'if [[ $BUILD == 0 ]]; then cpanm --verbose --installdeps --notest Pod::Coverage::TrustPod; fi'
   - 'if [[ $BUILD == 0 ]]; then cpanm --verbose --installdeps --notest Test::CPAN::Meta; fi'
   - 'if [[ $BUILD == 1 ]]; then cd .build/testing; fi'
   - 'if [[ $BUILD == 1 ]]; then export AUTHOR_TESTING=0; fi'
   - 'if [[ $BUILD == 1 ]]; then cpanm --verbose --installdeps --notest Pod::Weaver::Section::Contributors; fi'
   - 'if [[ $BUILD == 1 ]]; then cpanm --verbose --installdeps --notest Pod::Elemental::Transformer::List; fi'
   - 'if [[ $BUILD == 1 ]]; then cpanm --verbose --installdeps --notest Pod::Coverage::TrustPod; fi'
   - 'if [[ $BUILD == 1 ]]; then cpanm --verbose --installdeps --notest Test::CPAN::Meta; fi'
install:
   - 'if [[ $BUILD == 0 ]]; then cpanm --quiet --notest --skip-satisfied Dist::Zilla; fi'
   - "if [[ $BUILD == 0 ]]; then dzil authordeps          --missing | grep -vP '[^\\w:]' | xargs -n 5 -P 10 cpanm --quiet --notest; fi"
   - "if [[ $BUILD == 0 ]]; then dzil listdeps   --author --missing | grep -vP '[^\\w:]' | cpanm --verbose; fi"
   - 'if [[ $BUILD == 1 ]]; then cpanm --installdeps --verbose  .; fi'
script:
   - 'if [[ $BUILD == 0 ]]; then dzil smoke --release --author; fi'
   - 'if [[ $BUILD == 1 && -f Makefile.PL ]]; then perl Makefile.PL && make test;    fi'
   - 'if [[ $BUILD == 1 && -f Build.PL    ]]; then perl Build.PL    && ./Build test; fi'
   - 'if [[ $BUILD == 1 && ! -f Makefile.PL && ! -f Build.PL ]]; then  make test;    fi'
